<Window x:Class="WebTaddy.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WebTaddy.Views"
        xmlns:ui="clr-namespace:WebTaddy.UIComponents"
        xmlns:vm="clr-namespace:WebTaddy.ViewModels"
        mc:Ignorable="d"
        Title="MainView"
        Height="950" Width="1500"
        MinHeight="410" MinWidth="515"
        WindowStartupLocation="CenterScreen">
    
    <Grid>

        <ContentControl Content="{Binding CurrentContentViewModel}"    >
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:LayoutViewModel}">
                    <ui:Layout/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    
    </Grid>
    
</Window>
